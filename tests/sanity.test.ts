import { describe, expect, it } from "vitest";

describe("LLM observability quality lab sanity check", () => {
  it("runs the test environment correctly", () => {
    expect(true).toBe(true);
  });
});
